- Zips JS files into a form that can be transmitted over the wire in a single stream without round trips.
- Like [[Promise Pipelining]] for module loading.
- Can include non-JS assets
    - binaries
    - fixtures
