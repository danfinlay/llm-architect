public interface CompilerSubcomponent {
    /**
     * Compiles the code for the custom chess piece into a usable format for the chess game.
     * @param code The code for the custom chess piece.
     * @return The compiled code.
     */
    public String compile(String code);
    
    /**
     * Communicates with the Chess Game Subcomponent to add the custom piece to the game.
     * @param piece The custom chess piece to add to the game.
     */
    public void addCustomPiece(ChessPiece piece);
}