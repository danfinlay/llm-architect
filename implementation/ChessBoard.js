// One important improvement:
// Add a function to the Game Board Component that checks if a move is valid before allowing it to be made.

function isMoveValid(piece, fromSquare, toSquare) {
  // implementation code here
}